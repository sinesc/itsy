// Naive recursive Fibonacci -- stresses function call overhead.

fn fib(n: i32) -> i32 {
    if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn bench() {
    for _ in 0..6 {
        fib(17);
    }
}
