// Benchmark: marshalling an array of structs (Point) across the boundary.
// Each iteration gets [Point] from Rust (50 elements) and passes it back for summing.

use BenchAPI::{make_points, points_sum};
use BenchAPI::Point;

fn bench() {
    let arr = make_points(50);
    let _sum = points_sum(arr);
}
